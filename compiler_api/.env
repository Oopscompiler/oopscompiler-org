# Copy to .env and adjust for your machine
# This should match your PHP app DB (design2 uses db: coding, user: root)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=coding

# Optional: tune these if you want
# COMPILER_IMAGE=code-sandbox:latest
